# pihole_log_correlation
A script that correlates "query" and "blocked" log lines from the Pi-hole log by adding the client IP from the "query" line to the "blocked" line and outputs it to a seperate log file.
